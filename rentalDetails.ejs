<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Rental Details</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="rental-details-container">
      <h2><%= rental.title %></h2>
      <p><strong>Description:</strong> <%= rental.description %></p>
      <p>
        <strong>Price:</strong> Rp <%= rental.price !== null && rental.price !==
        undefined ? rental.price : 'N/A' %>
      </p>
      <p><strong>Size:</strong> <%= rental.size %></p>
      <p>
        <strong>Listed by:</strong> <%= rental.username || 'Unknown User' %>
      </p>

      <a href="/rentals/<%= rental.id %>/edit" class="btn">Edit Rental</a>
      <form
        action="/rentals/<%= rental.id %>/delete"
        method="POST"
        style="display: inline"
      >
        <button
          type="submit"
          class="btn delete-rental"
          onclick="return confirm('Are you sure you want to delete this rental?');"
        >
          Delete Rental
        </button>
      </form>
      <a href="/rentals" class="btn">Back to Rentals</a>
    </div>

    <style>
      .rental-details-container {
        max-width: 600px;
        margin: auto;
        padding: 20px;
        font-family: Arial, sans-serif;
        border: 1px solid #ccc;
        border-radius: 5px;
        background-color: #f9f9f9;
      }
      .btn {
        display: inline-block;
        margin-top: 10px;
        padding: 8px 12px;
        background-color: #007bff;
        color: #fff;
        text-decoration: none;
        border-radius: 5px;
      }
      .btn:hover {
        background-color: #0056b3;
      }
      .delete-rental {
        background-color: #dc3545;
      }
      .delete-rental:hover {
        background-color: #c82333;
      }
    </style>
  </body>
</html>
