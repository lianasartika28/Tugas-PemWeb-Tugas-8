<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Rental</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="form-container">
      <h2>Create New Rental</h2>
      <form action="/rentals/create" method="POST" class="rental-form">
        <label for="title">Title:</label>
        <input
          type="text"
          id="title"
          name="title"
          required
          class="form-input"
        />

        <label for="description">Description:</label>
        <textarea
          id="description"
          name="description"
          required
          class="form-input"
        ></textarea>

        <label for="price">Price:</label>
        <input
          type="number"
          id="price"
          name="price"
          required
          step="0.01"
          class="form-input"
        />

        <label for="size">Size:</label>
        <input type="text" id="size" name="size" required class="form-input" />

        <button type="submit" class="btn">Create Rental</button>
      </form>
      <a href="/rentals" class="btn">Back to Rentals</a>
    </div>

    <style>
      .form-container {
        max-width: 500px; /* Limits the width of the form */
        margin: auto; /* Centers the form */
        padding: 20px; /* Adds padding inside the form */
        border: 1px solid #ccc; /* Adds a border around the form */
        border-radius: 5px; /* Rounds the corners of the form */
        background-color: #f9f9f9; /* Light background color */
        font-family: Arial, sans-serif; /* Font style */
      }

      .form-container h2 {
        text-align: center; /* Center the heading */
      }

      .rental-form {
        display: flex; /* Use flexbox for layout */
        flex-direction: column; /* Arrange elements in a column */
      }

      .form-input {
        width: 100%; /* Full width for inputs */
        margin-bottom: 15px; /* Space between inputs */
        padding: 10px; /* Padding inside inputs */
        border: 1px solid #ccc; /* Border for inputs */
        border-radius: 5px; /* Rounding the corners of inputs */
        font-size: 16px; /* Font size for inputs */
        box-sizing: border-box; /* Includes padding and border in the total width */
      }

      .btn {
        padding: 10px 15px; /* Padding for buttons */
        background-color: #007bff; /* Button background color */
        color: #fff; /* Button text color */
        text-decoration: none; /* No underline */
        border-radius: 5px; /* Rounding the corners of buttons */
        border: none; /* Remove border */
        cursor: pointer; /* Pointer cursor on hover */
        font-size: 16px; /* Font size for buttons */
        text-align: center; /* Center button text */
      }

      .btn:hover {
        background-color: #0056b3; /* Darker shade on hover */
      }
    </style>
  </body>
</html>
